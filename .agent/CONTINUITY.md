# CONTINUITY

Facts only. No transcripts. If unknown, write UNCONFIRMED.
Add dated entries with provenance tags per AGENTS.md: [USER], [CODE], [TOOL], [ASSUMPTION].

## Snapshot

Goal: 2026-03-28 [USER] Define `skarbot-core` as a personalized claw-like assistant monorepo built on `pi.dev`, installable on a VM through repo-local setup, with a deliberately small feature set.
Now: 2026-04-09 [CODE] `docs-v5/runtime-v2.md` is still the active replacement draft for the runtime doc, and it now has finished prose for model providers, tasks and schedules, workspace cleanup, custom tools and skills, and memory and compaction. The next section, `Approvals and pause or resume behavior`, has been researched and drafted in chat but not patched yet.
Next: 2026-04-09 [CODE] Continue the section-by-section rewrite of `docs-v5/runtime-v2.md` by reviewing and patching the approvals section, then draft the final `Runtime boundaries` section and align any stale wording in `tools.md` and `architecture.md`.
Open Questions: 2026-04-09 [ASSUMPTION] The remaining unstable parts of `runtime-v2.md` are the approvals section wording and the final runtime boundaries section. It also remains undecided whether the final runtime docs should stay as one large file or split further into a `docs-v5/runtime/` subfolder.

## Done (recent)

- 2026-03-28 [TOOL] Imported `pi-mono`, `openclaw`, `nanoclaw`, `nanobot`, `nemoclaw`, and `oh-my-pi` as git submodules under `references/`.
- 2026-03-28 [CODE] Created the docs scaffold, continuity ledger, and draft ExecPlan, then replaced the earlier `project-definition` draft with focused docs for overview, providers, channels, web UI, users and threads, and runtime topics.
- 2026-03-28 [CODE] Recorded the core product shape: v1 surfaces, adapter-core architecture, `exe.dev` runtime baseline, thin web UI, dedicated-VM autonomy, PR-based git flow, file-first state, no-DB source-of-truth, no-heartbeat rule, and `vm-dotfiles` post-bootstrap setup contract.
- 2026-03-28 [CODE] Recorded the onboarding and membership direction: stable internal user ids, file-backed user records under `users/active`, `users/pending`, and `users/denied`, setup-created initial admin, app-managed membership on top of `exe.dev` login headers, and admin email notifications sent from the VM.
- 2026-03-29 [CODE] Finalized the task and thread model: one long-lived direct thread per user, one task-linked thread per scoped task, direct replies staying on the initiating surface, scheduled-task notifications as an owner-targeted list, task lifecycle through filesystem placement under `tasks/`, `tasks/inactive/`, and `tasks/failed/`, and per-thread workspaces under `~/skarbot/workspaces/`.
- 2026-03-29 [CODE] Finalized the capability model: repo-backed system capabilities under `~/skarbot/core/capabilities/`, user and candidate capabilities under `~/skarbot/state/capabilities/`, SDK-native `pi` tool packages for tools, grouped task-only `capabilities(list|inspect|get|promote)`, deterministic promotion validation, and candidate preview limited to the linked task thread.
- 2026-03-29 [CODE] Rewrote provider and model docs around one deployment-wide `openai-codex` auth profile, deployment default model `openai-codex/gpt-5.4`, per-thread remembered current model, and first-pass usage and switching rules grounded in `pi-coding-agent`.
- 2026-03-29 [CODE] Added `docs/runtime/execution-profiles.md` as the source of truth for runtime composition, centralized Skarbot on exactly two execution profiles, `direct-thread` and `task-thread`, and trimmed overlapping profile behavior out of overview, platform, tasks, and threads docs.
- 2026-03-29 [CODE] Added `docs/runtime/approvals.md` as the source of truth for approvals, centralized Skarbot on one shared approval system with `user` and `admin` approver classes, defined canonical reply grammar, routed user approvals to direct threads and admin approvals to `exe.dev` email, and trimmed approval drift out of autonomy, tasks, users, and web UI docs.
- 2026-03-29 [CODE] Tightened approvals and autonomy docs so admin approvals from user-owned work require explicit user escalation from the direct thread, blocked tasks pause while waiting for admin resolution, host package installs require admin approval, and `~/skarbot/core/` is treated as product code that the running agent must not edit.
- 2026-03-29 [CODE] Updated the runtime docs to capture two newly agreed grouped system tools: minimal thread-local `plan(...)` state (`get/create/update`) and minimal `subagents(...)` orchestration (`list/spawn/kill`) with one generic worker type and model-chosen Malayalee names as active handles.
- 2026-03-29 [CODE] Added `docs/runtime/tools.md` as the source of truth for the standard tool surface, documenting the split between `pi`'s built-in coding core and Skarbot's smaller shared and task-thread system layers, including `web_search` and `web_fetch`.
- 2026-04-03 [CODE] Created `docs-v5/` as the active review set copied from `docs-v4/`, then added `docs-v5/review-rules.md` to record the editing-pass rules.
- 2026-04-03 [CODE] Revised `docs-v5/README.md` and `docs-v5/product.md` to use `channels`, improve product-first wording, prefer `user thread` in prose, and align onboarding and approval behavior with the current direction.
- 2026-04-03 [CODE] Revised `docs-v5/architecture.md` to remove `instance.toml`, add explicit `pi` SDK attribution with pinned upstream links, trim redundant prose, and change the user-id rule to first-and-last-name-derived ids.
- 2026-04-04 [CODE] Completed the main `docs-v5/architecture.md` pass: tasks are now owner-scoped across state/threads/workspaces, approvals use one `source` field and UUID ids, recurring schedules use the owner's user-record timezone, user capabilities are flattened to `~/skarbot/state/capabilities/<user-id>/...`, attachments remain thread-owned with optional workspace materialization, and several redundant architecture-level sentences were removed.
- 2026-04-04 [CODE] Revised `docs-v5/runtime.md` to align with the `docs-v5` review rules and completed architecture decisions: `user-thread` replaced `direct-thread` in prose and profile naming, stale thread/task/capability paths were corrected to the owner-scoped layout, user approvals were aligned to web and SMS while admin approvals stayed on email, approval ids were updated from short codes to UUIDs, and inherited `pi` runtime behavior now cites pinned upstream permalinks for session headers, built-in tools, planning, subagents, and model switching.
- 2026-04-04 [CODE] Split detailed tool-contract material out of `docs-v5/runtime.md` into a new `docs-v5/tools.md`, then started a structural replacement draft in `docs-v5/runtime-v2.md` after concluding the old runtime doc mixed profiles, subsystem contracts, and lifecycle rules in a way that made the runtime hard to understand.
- 2026-04-04 [CODE] Reworked the top of `docs-v5/runtime-v2.md` into final-doc style: removed draft boilerplate and numbered headings, replaced the intro with a short canonical opening, rewrote the channel and thread routing section around user-thread control plus task-thread execution, and replaced the run-lifecycle outline with concise prose sections for start, execute, pause, resume, and complete.
- 2026-04-08 [CODE] Rewrote the `docs-v5/runtime-v2.md` model section as `Model Providers`, clarifying one deployment-wide OpenAI subscription auth path at `~/.skarbot/auth.json`, default thread model `openai-codex/gpt-5.4` with `medium` reasoning effort, per-thread persisted model selection, and optional task-file model override for task threads.
- 2026-04-08 [CODE] Rewrote the `docs-v5/runtime-v2.md` memory and compaction section in the same shorter product-spec style: every workspace has its own `MEMORY.md`, the runtime may run one silent memory flush turn before compaction, default thresholds are `reserveTokens: 20000`, `keepRecentTokens: 20000`, and `memoryFlushThreshold: 4000`, and overflow recovery may compact and retry once.
- 2026-04-09 [CODE] Researched the remaining approvals section against `docs/runtime/approvals.md`, `docs-v5/runtime.md`, `docs-v5/architecture.md`, and OpenClaw approval docs/code. Drafted an in-chat replacement for `## Approvals and pause or resume behavior` covering two approver classes (`user`, `admin`), UUID-based `approve|deny|feedback` reply grammar, pause-on-approval behavior, resume semantics for `approve|deny|feedback|expired`, terminal feedback behavior, and the rule that user-owned work must ask the user before escalating to an admin approval.
- 2026-04-08 [CODE] Rewrote the `docs-v5/runtime-v2.md` tasks and schedules section into direct prose with explicit task start modes, owner-scoped task paths, task-thread and workspace reuse rules, deterministic heavy-directory pruning for inactive or failed task workspaces, and admin-only full workspace deletion through `.deleteworkspace`.
- 2026-04-08 [CODE] Replaced the old `Capability-building runs` outline in `docs-v5/runtime-v2.md` with a `Custom tools and skills` section: tool and skill creation or update now happens in `task-thread`s, starts from `drafts/` first and `active/` second, supports task-local preview, runs deterministic validation, writes validated results to `drafts/`, and activates only after `user-thread` approval.
- 2026-04-08 [CODE] Updated `docs-v5/tools.md` so `capabilities(...)` now distinguishes `user-thread` review and approval actions (`review|approve|deny|feedback`) from task-thread build actions (`get|test|validate|stage`).
- 2026-04-08 [CODE] Updated `docs-v5/architecture.md` so task records may optionally carry a `capability` object with `kind` and `name`, custom tools and skills are explicitly described as living under the capabilities tree, and moving a draft to `active/` is defined as a user-thread approval action rather than a task-thread action.
- 2026-04-08 [CODE] Added a repo-specific docs-style note to `AGENTS.md`: docs should read like a product spec or operator manual, start with concrete behavior, prefer product nouns, keep sentence count low, avoid “why this section exists” prose, and stay close to the user's phrasing.
- 2026-04-04 [TOOL] Added `references/hermes-agent` as a read-only git submodule for lifecycle research, pinned at commit `931624feda96be2c2a8bdd0ea48c6c5d2f3db87b`, and started comparing its executor, gateway interrupt handling, and cron scheduler against the existing reference set with the specific question of how each runtime keeps work alive until completion.
- 2026-03-29 [CODE] Updated the runtime tools and providers docs to define a minimal grouped `model(...)` surface: `model()`, `model(list)`, and `model(set, provider, model)`, grounded in `pi` session model switching and session usage stats.
- 2026-03-29 [CODE] Updated the runtime docs to define `schedule(...)` as a user-owned direct-thread tool with `list/set/edit/delete`, recurring schedules bound to dedicated task threads, one-off schedules using throwaway task threads, explicit user-approval requirements for schedule mutations, and default timezone taken from the user record.
- 2026-03-29 [CODE] Updated the provider and setup docs to keep deployment-wide Codex OAuth outside portable `~/skarbot/state`, with `~/.skarbot/auth.json` as the canonical host-managed auth path.
- 2026-03-29 [CODE] Normalized the VM root layout in docs and continuity to `~/skarbot/core`, `~/skarbot/state`, and `~/skarbot/workspaces`, while keeping host-managed model auth at `~/.skarbot/auth.json`.
- 2026-03-29 [CODE] Defined the baseline automatic compaction model as rolling summary plus recent tail: threads remain append-only on disk, compaction summaries persist in thread history, later compactions incorporate the previous summary plus older post-compaction messages with truncated tool serialization, and users should not need to think about compaction during normal use.
- 2026-03-29 [CODE] Simplified memory to plain workspace-local `MEMORY.md` files maintained through the normal coding tools, removed the dedicated runtime memory tool idea, and removed task JSON `memory` from the active docs.

## Working set

- docs/overview.md
- docs/providers.md
- docs/channels.md
- docs/web-ui.md
- docs/users-and-threads.md
- docs/runtime/platform-and-setup.md
- docs/runtime/execution-profiles.md
- docs/runtime/approvals.md
- docs/runtime/autonomy.md
- docs/runtime/tasks.md
- docs/runtime/memory.md
- docs/research/reference-projects.md
- .agent/CONTINUITY.md
- .agent/execplans/draft/EP-2026-03-28__skarbot-foundation.md

## Decisions

- D001 ACTIVE: 2026-03-28 [USER] `skarbot-core` will be a personalized, `pi.dev`-based claw assistant rather than a general-purpose assistant platform.
- D002 ACTIVE: 2026-03-28 [USER] Upstream references are stored as read-only git submodules pinned to latest `HEAD` at add time.
- D003 SUPERSEDED: 2026-03-28 [USER] This work is tracked with an active ExecPlan during research and initial scaffolding.
- D004 ACTIVE: 2026-03-28 [ASSUMPTION] `NemoClaw` is included as a security/deployment research reference even though it is not a likely first-version dependency.
- D005 SUPERSEDED: 2026-03-28 [USER] The desired v1 surfaces are terminal, web UI, email, SMS via ClickSend, and scheduled jobs / cron jobs.
- D006 ACTIVE: 2026-03-28 [USER] First install must support inbound email and inbound SMS; notification-only phase 1 is rejected.
- D007 ACTIVE: 2026-03-28 [USER] The preferred architecture is one lightweight adapter-based core, following the `nanoclaw` pattern more closely than a heavier OpenClaw-style control plane.
- D008 ACTIVE: 2026-03-28 [USER] ClickSend is accepted for SMS, while email should use a separate integration path.
- D009 ACTIVE: 2026-03-28 [USER] Email is inbound-only for v1 and should use the inbox already granted by the `exe.dev` VM.
- D010 ACTIVE: 2026-03-28 [USER] v1 deploys to one Linux VM on `exe.dev`; local testing is useful but not a first-release support requirement.
- D011 ACTIVE: 2026-03-28 [USER] `exe.dev` should be treated as a standardized platform for the first deployment path because it simplifies GitHub access, web exposure, SSH access, and inbound email.
- D012 SUPERSEDED: 2026-03-28 [USER] The agent may autonomously clone and modify repos explicitly attached to the VM via `exe.dev` integrations, but the user merges to `main`.
- D013 ACTIVE: 2026-03-28 [USER] The git workflow should minimize branch sprawl via a stable working branch per repo plus short-lived PR branches.
- D014 ACTIVE: 2026-03-28 [USER] Privileged approvals use a private `exe.dev`-authenticated page, with SMS escalation after one hour rather than SMS approvals.
- D014A ACTIVE: 2026-03-28 [USER] The approvals page should be reachable only by the admin user through `exe.dev` auth, not by every authenticated user.
- D015 ACTIVE: 2026-03-28 [USER] The dedicated VM is autonomous by default, including package installs and process changes, except for `exe.dev` sharing/integration changes, new SMS recipients, and destructive actions outside configurable owned roots.
- D016 ACTIVE: 2026-03-28 [USER] The v1 web UI includes a Skarbot home page, approvals queue, chat-style thread viewer, and basic logs/status page.
- D017 ACTIVE: 2026-03-28 [USER] Tasks are represented as specific threads rather than as a separate top-level product surface.
- D018 SUPERSEDED: 2026-03-28 [USER] Personal inboxes are per authenticated `exe.dev` user, and the UI should scope visibility accordingly.
- D019 ACTIVE: 2026-03-28 [USER] The system should include an onboarding flow that links inbound phone/email identities to authenticated users.
- D020 SUPERSEDED: 2026-03-28 [USER] The user's wife should have her own inbox and be able to create workflow threads in v1.
- D021 SUPERSEDED: 2026-03-28 [USER] Live Skarbot state must stay outside the repo, with deterministic policy in code/config and plain directory shards for user and workflow memory.
- D022 SUPERSEDED: 2026-03-28 [USER] The default external Skarbot state root on the VM should be `~/skarbot-state`.
- D023 ACTIVE: 2026-03-28 [USER] V1 should skip a standalone heartbeat concept; recurring checks stay with explicit workflows and scheduled jobs.
- D024 SUPERSEDED: 2026-03-28 [USER] V1 runtime state should be file-first under `~/skarbot-state`, with no database as the source of truth.
- D025 SUPERSEDED: 2026-03-28 [USER] User inbox shards consolidate once after 30 minutes idle per activity window, and workflow shards consolidate after each succeeded run.
- D026 ACTIVE: 2026-03-28 [USER] V1 requires no additional day-one integrations beyond ClickSend, the `exe.dev` inbox, and `exe.dev` GitHub access.
- D027 SUPERSEDED: 2026-03-28 [USER] The main VM roots should be `~/skarbot-core` for repo code, `~/skarbot-state` for canonical state, and `~/skarbot-data` for task execution space and artifacts.
- D028 ACTIVE: 2026-03-28 [USER] V1 should not add extra sandboxing inside the dedicated VM beyond the current approval rules and owned-root boundaries.
- D029 ACTIVE: 2026-03-28 [USER] `vm-dotfiles` is the standard prerequisite bootstrap for the `exe.dev` VM; `skarbot-core` begins at the Skarbot-specific post-bootstrap setup step.
- D030 ACTIVE: 2026-03-28 [USER] `skarbot-core` post-bootstrap setup should create and validate state/data roots, install repo-local dependencies including `pi-agent`, write config, wire `exe.dev` assumptions, start runtime and scheduled-job wiring, run health checks, and register the initial admin by email and phone.
- D031 ACTIVE: 2026-03-28 [USER] Setup should use an interactive first-run flow that writes config, while also supporting non-interactive config-file and env-driven reruns and automation.
- D032 SUPERSEDED: 2026-03-28 [USER] Generated instance config should live under `~/skarbot-state/config/` by default, while the repo holds only templates and checked-in defaults.
- D033 SUPERSEDED: 2026-03-28 [USER] The main generated config file should use `TOML`, with environment variables reserved for secrets and temporary overrides.
- D034 ACTIVE: 2026-03-28 [USER] The main generated config file should use `TOML`; secrets should not be written into repo config or generated local secret files, ClickSend should prefer `exe.dev` proxy/integration endpoints, and model providers may use local CLI or subscription-backed auth outside repo config.
- D035 ACTIVE: 2026-03-28 [USER] V1 should use one main generated `TOML` file rather than multiple generated config files.
- D036 ACTIVE: 2026-03-28 [USER] Defer the exact top-level `TOML` section layout until later in the docs process, after the product behavior and workflow definitions are clearer.
- D037 ACTIVE: 2026-03-28 [USER] V1 should treat natural-language workflow scaffolding as a first-class capability; a fixed initial workflow list is less important than being able to turn requests like a morning briefing into recurring workflows.
- D038 ACTIVE: 2026-03-28 [USER] During docs and research work, the foundation ExecPlan should remain a draft plan and only be promoted to active when implementation work is approved.
- D039 SUPERSEDED: 2026-03-28 [USER] Newly scaffolded recurring tasks should start as draft task records in runtime state, containing the request, drafted instructions, owner, intended channel if known, missing questions, proposed schedule if inferred, and a draft or awaiting-approval status; no live schedule should be created until approval.
- D040 ACTIVE: 2026-03-28 [USER] Drafted recurring tasks should be backed by files on disk so they can be inspected or edited over SSH, while the web UI remains able to approve or reject them.
- D041 SUPERSEDED: 2026-03-28 [USER] Approving a drafted recurring task from the admin-only web UI should activate its live schedule immediately after validation.
- D042 SUPERSEDED: 2026-03-28 [USER] The simplest v1 recurring-task model is one JSON file per task under `~/skarbot-state/tasks/`, with a `status` field such as `draft`, `active`, `rejected`, or `paused`, instead of separate draft and active directories.
- D043 ACTIVE: 2026-03-28 [USER] Ordinary recurring tasks should not require human approval; if information is missing, the agent should ask follow-up questions and should not create the task file yet.
- D044 SUPERSEDED: 2026-03-28 [USER] The recurring-task file model should use one JSON file per task under `~/skarbot-state/tasks/` with only two statuses in v1: `active` and `disabled`.
- D045 SUPERSEDED: 2026-03-28 [USER] The agent should be armed with a `create-task` skill that defines the JSON shape and task path; other task changes should happen by editing the file rather than through a separate task action API.
- D046 ACTIVE: 2026-03-28 [USER] The agent should be armed with one `tasks` skill that defines the task JSON shape, task path, and how task files are created, inspected, and edited; later task changes should still happen by editing the file rather than through a separate task action API.
- D047 ACTIVE: 2026-03-28 [USER] Task status in v1 should mean only the current task file state (`active` or `disabled`), not a richer execution-status system.
- D048 ACTIVE: 2026-03-28 [USER] The built-in runtime skill set should stay very small in v1: `tasks` plus `memory`, with broader domain skills deferred.
- D049 ACTIVE: 2026-03-28 [USER] The catch-all project definition should be replaced by focused docs, with `overview` instead of `project-definition`, plus separate docs for providers, channels, web UI, and runtime topics.
- D050 SUPERSEDED: 2026-03-28 [USER] The `memory` skill should write durable facts to the correct filesystem-backed scope, search the relevant `HISTORY.ndjson` when needed, honor explicit “remember this” requests, support lightweight recall, and prefer filesystem operations wherever practical.
- D051 SUPERSEDED: 2026-03-28 [USER] Repo-owned baseline instructions, skill contracts, and product definition are not memory; v1 learned memory should use deployment-wide `~/skarbot-state/MEMORY.md`, per-user `~/skarbot-state/users/<id>/MEMORY.md`, and task-specific context stored directly in task JSON files.
- D052 SUPERSEDED: 2026-03-28 [USER] User memory should consolidate once after 30 minutes idle per activity window, while task-specific context should be refreshed in task JSON after successful runs instead of using a separate workflow-memory shard.
- D053 SUPERSEDED: 2026-03-28 [USER] Each task JSON file should include a dedicated `memory` section for durable lessons and improvements; it should not accumulate routine outputs or run-by-run logs.
- D054 SUPERSEDED: 2026-03-28 [USER] Routine recurring-task outputs should be persisted in linked task-thread history rather than in task `memory`.
- D055 SUPERSEDED: 2026-03-28 [USER] In v1, each recurring task should map to exactly one workflow thread, and the web UI should clearly distinguish task threads from user inbox threads.
- D056 ACTIVE: 2026-03-28 [USER] V1 should use one shared thread concept: one long-lived direct thread per human user, one task-linked thread per recurring task, and UI grouping derived from whether a thread is linked to a task.
- D057 ACTIVE: 2026-03-28 [USER] The user's wife should be supported as a first-class non-admin user who has her own long-lived direct thread and can create recurring-task threads in v1.
- D058 ACTIVE: 2026-03-28 [USER] Replies to direct interactions should use the initiating surface rather than mirroring across every surface.
- D059 SUPERSEDED: 2026-03-28 [USER] Recurring tasks should have an explicit notification method rather than inferring one from the originating surface.
- D060 SUPERSEDED: 2026-03-28 [USER] `none` is a valid recurring-task notification method for workflows that should keep results only in their own task thread.
- D061 SUPERSEDED: 2026-03-28 [USER] Beyond `none`, the only recurring-task notification methods supported in v1 should be `direct-thread` and `sms`.
- D062 ACTIVE: 2026-03-28 [USER] Recurring tasks should have an explicit `notification` list rather than one inferred or singular notification method; `[]` means no external delivery, and supported v1 entries are `direct-thread` and `sms`.
- D063 ACTIVE: 2026-03-28 [USER] In v1, recurring-task notifications should always target the task owner.
- D064 ACTIVE: 2026-03-28 [USER] Tasks should be the general unit of scoped work in v1, not just recurring workflows; a task with no `schedule` starts immediately in its own task thread, a one-off task uses `schedule.at`, and a recurring task uses `schedule.cron` plus `schedule.timezone`.
- D065 SUPERSEDED: 2026-03-28 [USER] Task lifecycle in v1 should be carried by filesystem placement rather than a `status` field: active tasks live in `~/skarbot-state/tasks/`, inactive tasks in `~/skarbot-state/tasks/inactive/`, and failed tasks in `~/skarbot-state/tasks/failed/`.
- D066 ACTIVE: 2026-03-28 [USER] The task JSON does not need a separate `channel` field in v1.
- D067 ACTIVE: 2026-03-28 [USER] The task JSON should continue to carry an explicit `owner` field in v1 unless a separate task-thread ownership contract is introduced.
- D068 ACTIVE: 2026-03-28 [USER] The task filename should be the canonical identifier in v1 instead of a separate task `id` field inside the JSON.
- D069 ACTIVE: 2026-03-28 [USER] Task filenames in v1 should be human-readable slugs derived from the task title.
- D070 ACTIVE: 2026-03-28 [USER] Once created, a task filename should stay stable even if the task title changes later.
- D071 ACTIVE: 2026-03-28 [USER] If a slug filename collision occurs within the destination directory, Skarbot should append `-2`, `-3`, and so on.
- D072 ACTIVE: 2026-03-28 [USER] The task JSON should keep an explicit `title` field in v1 as the human-facing label even though the filename is canonical.
- D073 ACTIVE: 2026-03-28 [USER] The task JSON should keep `instructions` as one plain string in v1 rather than a richer structured field.
- D074 SUPERSEDED: 2026-03-28 [USER] The task `memory` section should be a simple list of short durable lesson strings in v1.
- D075 ACTIVE: 2026-03-28 [USER] The task JSON should not include explicit `created_at` or `updated_at` fields in v1; filesystem metadata and thread or run history are sufficient.
- D076 ACTIVE: 2026-03-28 [USER] The task `owner` field should store a stable internal Skarbot user id rather than an email address or phone number.
- D077 SUPERSEDED: 2026-03-28 [USER] Skarbot membership should be app-managed with one file-backed user record per member under `~/skarbot-state/users/active/`, `~/skarbot-state/users/pending/`, and `~/skarbot-state/users/denied/`.
- D078 ACTIVE: 2026-03-28 [USER] The main app may sit behind a public `exe.dev` proxy, but protected routes should force `exe.dev` login, use `X-ExeDev-UserID` and `X-ExeDev-Email` as identity signals, and gate access through Skarbot membership records rather than `exe.dev` sharing alone.
- D079 ACTIVE: 2026-03-28 [USER] Initial setup should create the admin user record directly, while later member requests should flow through onboarding plus admin approval and may notify the admin through outbound email sent from the `exe.dev` VM.
- D080 ACTIVE: 2026-03-29 [USER] Per-user tool capabilities in v1 should be SDK-native local `pi` packages or extensions rather than `tool.json` executable bundles; those packages may still call colocated scripts, bundled binaries, system executables, local dependencies, and approved HTTP proxies from their extension code.
- D081 ACTIVE: 2026-03-29 [USER] Per-user tool packages should follow the same flexible entrypoint-driven layout as `pi` extensions and packages rather than a Skarbot-specific fixed internal tree.
- D082 ACTIVE: 2026-03-29 [USER] Active user capabilities should be resolved at the start of each owned run or turn and should become available from the next direct-thread turn or task run onward, not only on a long-lived session start.
- D083 SUPERSEDED: 2026-03-29 [USER] Per-turn capability resolution should scan repo-backed system capability roots under `~/skarbot-core/capabilities/` plus the owner's active capability roots under `~/skarbot-state/capabilities/users/<user-id>/`.
- D084 ACTIVE: 2026-03-29 [USER] Skarbot should have system skills and system tools loaded before per-user capabilities, with user capabilities taking precedence on name collisions in v1.
- D085 SUPERSEDED: 2026-03-29 [USER] System capabilities are repo-backed product code and should live in `~/skarbot-core/capabilities/skills/` and `~/skarbot-core/capabilities/tools/`, while user capabilities remain runtime state under `~/skarbot-state/capabilities/`.
- D086 SUPERSEDED: 2026-03-29 [USER] Setup should validate the repo-backed system capability roots under `~/skarbot-core/capabilities/` and should not create them dynamically.
- D087 ACTIVE: 2026-03-29 [USER] One `tools/<name>/` SDK-native package may register multiple named tools in v1; the package directory is a capability bundle identifier rather than a 1:1 tool-name binding.
- D088 ACTIVE: 2026-03-29 [USER] Candidate capabilities should pass a deterministic validation step exposed through a Skarbot system tool before they are eligible for promotion.
- D089 SUPERSEDED: 2026-03-29 [USER] The smallest v1 capability-management surface should use only two task-only system tools: `ask-user(question)` and grouped `capabilities(action, kind, name?, path?)`.
- D090 SUPERSEDED: 2026-03-29 [USER] In v1, grouped `capabilities` actions should be limited to `list`, `inspect`, `get`, and `promote`; `promote` should run deterministic validation internally and fail fast back to the task when invalid.
- D091 ACTIVE: 2026-03-29 [USER] Candidate capabilities should be preview-loadable only in their linked capability-building task thread and should not be auto-loaded into the owner's ordinary direct thread before approval.
- D092 ACTIVE: 2026-03-29 [USER] V1 should support exactly one model provider, `openai-codex`, using OpenAI Codex subscription auth rather than API-key auth.
- D093 ACTIVE: 2026-03-29 [USER] Model auth is the one planned v1 exception to the general `exe.dev` proxy or integration-backed secret rule; the admin should establish one deployment-wide Codex OAuth profile on the VM and all Skarbot sessions should share it.
- D094 ACTIVE: 2026-03-29 [USER] The deployment default model should be `openai-codex/gpt-5.4`, with no model fallbacks in v1.
- D095 ACTIVE: 2026-03-29 [USER] Model switching should be remembered per thread through session history rather than as one mutable global user or deployment preference; new threads start from the deployment default model.
- D096 ACTIVE: 2026-03-29 [USER] Skarbot should define exactly two execution profiles, `direct-thread` and `task-thread`; scheduled work and capability review remain task-thread cases rather than separate profile types.
- D097 ACTIVE: 2026-03-29 [USER] Skarbot approvals should use one shared approval system with two approver classes, `user` and `admin`; user approvals resolve in the user's direct thread, admin approvals resolve through the admin `exe.dev` email path, and the canonical approval grammar is `approve <id>`, `deny <id>`, `feedback <id>: <text>`.
- D098 ACTIVE: 2026-03-29 [USER] Admin approvals originating from user-owned work must require explicit user escalation from the user's direct thread; the agent may not create those admin approvals on its own, and blocked tasks should pause until the admin approval resolves.
- D099 SUPERSEDED: 2026-03-29 [USER] Host package installs require admin approval, and `~/skarbot-core/` is product code that the running agent must not edit at runtime.
- D100 ACTIVE: 2026-03-29 [USER] Skarbot v1 should not introduce a first-class `attached repo` concept.
- D101 SUPERSEDED: 2026-03-29 [USER] All task-thread runs should expose the same small task-only system tool surface in v1, including `ask-user(question)` and grouped `capabilities(action, kind, name?, path?)`.
- D102 ACTIVE: 2026-03-29 [USER] `plan(...)` should be minimal and thread-local: `plan()` returns the current plan, `plan(create, steps)` creates the one active plan for the thread, and `plan(update, id, status)` updates step status. Only one plan may exist per thread at a time; once all steps are `completed`, the active plan is considered complete and may be removed from active state.
- D103 ACTIVE: 2026-03-29 [USER] `subagents(...)` should use one generic spawned worker type in v1, with no separate internal id; the model should choose a cute Malayalee name as the active handle, and duplicate active names in the same thread are disallowed.
- D104 ACTIVE: 2026-03-29 [USER] Skarbot should treat `pi`'s coding core as substrate rather than redefining it; the documented standard tool surface should add first-class `web_search` and `web_fetch`, keep `plan(...)` and `subagents(...)` as small grouped tools, and leave `model(...)` and `schedule(...)` for separate definition.
- D105 ACTIVE: 2026-03-29 [USER] `model(...)` should be a minimal grouped tool with `model()`, `model(list)`, and `model(set, provider, model)`. It should operate at thread scope, surface current-thread usage and cost, list configured available models, and persist thread model changes without changing the deployment default model.
- D106 ACTIVE: 2026-03-29 [USER] `schedule(...)` should be a user-owned direct-thread tool with `schedule()`, `schedule(set, at)`, `schedule(set, cron)`, `schedule(edit, id, at)`, `schedule(edit, id, cron)`, and `schedule(delete, id)`. Schedule handles should be stable human-readable names unique per user. Recurring schedules bind to dedicated task threads, one-off schedules use throwaway task threads, timezone should default from the user record where possible, and schedule mutations require explicit user approval.
- D107 SUPERSEDED: 2026-03-29 [USER] Deployment-wide Codex OAuth should not live in portable `~/skarbot-state`; the canonical host-managed model-auth path should be `~/.skarbot/auth.json`.
- D108 ACTIVE: 2026-03-29 [USER] The VM root layout should be `~/skarbot/core/`, `~/skarbot/state/`, and `~/skarbot/workspaces/`, with deployment-wide model auth at `~/.skarbot/auth.json` so auth stays outside the agent-visible Skarbot tree and portable state remains separate from host-managed secrets.
- D109 ACTIVE: 2026-03-29 [USER] Thread compaction should be automatic and mostly invisible to the user, using a rolling-summary-plus-recent-tail model: the latest compaction summary stays in thread history, later compactions incorporate that summary plus older messages since the last compaction point with truncated tool serialization, full thread history remains append-only on disk, and overflow recovery should compact and retry automatically once.
- D110 ACTIVE: 2026-03-29 [USER] Compaction events should be visible only in thread history and the web UI thread viewer, and terminal should stay out of scope for v1.
- D111 ACTIVE: 2026-03-29 [USER] Memory should stay file-first and workspace-local in v1: each direct-thread or task-thread workspace should keep a `MEMORY.md` file maintained through the normal coding tools and the `memory` skill, there should be no dedicated runtime memory tool, and searchable thread history should be designed later as a separate concern.
- D112 ACTIVE: 2026-04-08 [USER] Runtime docs should use short product-spec language close to the user's phrasing rather than explanatory architecture prose; avoid padding, meta-explanation, and “why this section exists” framing.
- D113 ACTIVE: 2026-04-08 [USER] The runtime section previously called capability-building work should instead be framed as `Custom tools and skills`.
- D114 ACTIVE: 2026-04-08 [USER] Users can add custom tools and skills beyond the default Skarbot baseline; that work happens in task threads, starts from `drafts/` first and `active/` second, supports task-local preview, stages validated results to `drafts/`, and activates only after user-thread approval.
- D115 ACTIVE: 2026-04-08 [USER] `capabilities(...)` remains the grouped runtime tool name, but it should split into task-thread build actions (`get`, `test`, `validate`, `stage`) and user-thread review actions (`review`, `approve`, `deny`, `feedback`) instead of the earlier `list|inspect|get|promote` shape.
- D116 ACTIVE: 2026-04-08 [USER] Task records may carry an optional `capability` object with `kind` and `name` so custom tool and skill tasks can be identified deterministically.
- D117 ACTIVE: 2026-04-08 [USER] Task workspaces are retained by default, but the runtime should deterministically prune known heavy generated directories when tasks move to `inactive/` or `failed/`; recurring task workspaces may also be pruned when disabled; full workspace deletion stays admin-only via `.deleteworkspace`.

## Receipts

- 2026-03-28T14:18Z [TOOL] `pi-mono` positions itself as a minimal terminal coding harness with SDK/RPC seams and explicitly omits built-in subagents/plan mode. Source: `https://github.com/badlogic/pi-mono`, local path `references/pi-mono/packages/coding-agent/README.md`.
- 2026-03-28T14:18Z [TOOL] `openclaw` is the primary broad `pi`-based personal assistant reference, with onboarding, gateway, channels, and DM pairing security defaults. Source: `https://github.com/openclaw/openclaw`, local path `references/openclaw/README.md`.
- 2026-03-28T14:18Z [TOOL] `nanoclaw` emphasizes a small understandable codebase plus container isolation and bespoke customization. Source: `https://github.com/qwibitai/nanoclaw`, local path `references/nanoclaw/README.md`.
- 2026-03-28T14:18Z [TOOL] `nanobot` positions itself as an ultra-lightweight OpenClaw-inspired assistant and recently removed `litellm` after a supply-chain concern. Source: `https://github.com/HKUDS/nanobot`, local path `references/nanobot/README.md`.
- 2026-03-28T14:18Z [TOOL] `NemoClaw` is an alpha OpenShell/OpenClaw reference stack for safer always-on assistants. Source: `https://github.com/NVIDIA/NemoClaw`, local path `references/nemoclaw/README.md`.
- 2026-03-28T14:18Z [TOOL] `oh-my-pi` is the canonical repo at `can1357/oh-my-pi`, a `pi-mono` fork with LSP, Python, commit tooling, and other harness enhancements. Source: `https://github.com/can1357/oh-my-pi`, local path `references/oh-my-pi/README.md`.
- 2026-03-28T14:18Z [TOOL] Imported submodule SHAs: `pi-mono` `576e5e1a2fbe1abbbad96b696f4058cffd8391ca`, `openclaw` `23772bb785731126d723fe1d82cf662ba625ab6c`, `nanoclaw` `29839464bff39b7672c0a7504d9bb7649a79c5f1`, `nanobot` `c8c520cc9a4dbe619eb3f21200dc40971a36b665`, `nemoclaw` `eb4ba8c486ad97ea3ca67bea388690b641dc134e`, `oh-my-pi` `82b57365d421d05237149a70472444a455bd6d88`. Source: `git submodule status`.
- 2026-03-28T14:38Z [TOOL] `nanoclaw` uses a lightweight channel registry plus a shared `Channel` interface and router, which matches the desired adapter-based architecture. Source: local paths `references/nanoclaw/src/channels/registry.ts`, `references/nanoclaw/src/types.ts`, `references/nanoclaw/src/router.ts`, `references/nanoclaw/docs/SPEC.md`.
- 2026-03-28T14:38Z [TOOL] ClickSend exposes inbound SMS automations suitable for webhook-style SMS ingress, but its current email docs emphasize sending and note transactional email is no longer offered to new customers. Source: `https://developers.clicksend.com/docs/automations/sms/other/create-sms-inbound-automation`, `https://developers.clicksend.com/docs/messaging/email/other/send-email`.
- 2026-03-28T14:44Z [TOOL] `exe.dev` docs confirm persistent VMs, built-in HTTPS proxying, VM inbox delivery to Maildir, per-repo GitHub integrations without tokens on the VM, and agent-oriented docs. Sources: `https://exe.dev/llms.txt`, `https://exe.dev/docs/what-is-exe.md`, `https://exe.dev/docs/proxy.md`, `https://exe.dev/docs/receive-email.md`, `https://exe.dev/docs/integrations-github.md`, `https://exe.dev/docs/agent-skill.md`.
- 2026-03-28T16:03Z [TOOL] Second-pass memory review: `nanobot` provides the cleanest `MEMORY.md` plus append-only history pattern for v1, while `pi-mono/mom` provides the clearest split between prompt context and searchable logs; `openclaw` and `oh-my-pi` are useful but heavier than the current target. Sources: local paths `references/nanobot/nanobot/agent/memory.py`, `references/nanobot/nanobot/agent/context.py`, `references/pi-mono/packages/mom/src/context.ts`, `references/pi-mono/packages/mom/README.md`, `references/openclaw/extensions/memory-core/src/memory/manager.ts`, `references/oh-my-pi/docs/memory.md`.
- 2026-03-28T16:15Z [TOOL] Heartbeat follow-up review: `nanobot` and `openclaw` use heartbeat as a periodic wake/checklist mechanism, not as the memory-consolidation mechanism; `oh-my-pi` memory runs via startup/manual pipeline instead. Sources: local paths `references/nanobot/README.md`, `references/openclaw/docs/gateway/heartbeat.md`, `references/openclaw/docs/automation/cron-vs-heartbeat.md`, `references/oh-my-pi/docs/memory.md`.
- 2026-03-28 [TOOL] Workspace-layout review: the reference projects generally use a single workspace/data root plus subdirectories such as `scratch`, `attachments`, or per-channel/workflow areas rather than a broad top-level `projects` convention. Sources: local paths `references/pi-mono/packages/mom/README.md`, `references/openclaw/docs/start/openclaw.md`, `references/nanoclaw/src/container-runner.ts`, `references/nanobot/README.md`.
- 2026-03-28 [TOOL] `vm-dotfiles` README confirms a rerunnable `install.sh` for an `exe.dev`-style Ubuntu/Debian VM and positions itself as the source of truth for shell, tmux, CLI tooling, and agent CLIs before project-specific setup. Source: `https://raw.githubusercontent.com/Skarian/vm-dotfiles/main/README.md`.
- 2026-03-28 [TOOL] `exe.dev` exposes an HTTP Proxy integration that injects headers into outbound requests from the VM, which fits the desired “no raw ClickSend secret in Skarbot config” model. Sources: `https://exe.dev/docs/integrations-http-proxy.md`, `https://exe.dev/docs/proxy.md`.
- 2026-03-28 [CODE] Reclassified the foundation ExecPlan from active to draft and recorded the approved recurring-task model in the canonical docs set. Sources: `.agent/execplans/draft/EP-2026-03-28__skarbot-foundation.md`, `docs/overview.md`, `docs/runtime/tasks.md`.
- 2026-03-28 [TOOL] Reference pass on scheduling storage shows no single canonical claw format: `nanoclaw` uses SQLite task records, `openclaw` uses a persisted JSON cron store, `mom` uses one JSON event file per scheduled item, and `nanobot` presents cron mainly as tool calls. Source paths: `references/nanoclaw/src/db.ts`, `references/openclaw/docs/automation/cron-jobs.md`, `references/pi-mono/packages/mom/docs/events.md`, `references/nanobot/nanobot/skills/cron/SKILL.md`.
- 2026-03-28 [TOOL] Follow-up task-thread review: OpenClaw isolates noisy scheduled work into dedicated cron sessions, while `mom`, `nanobot`, and `nanoclaw` all distinguish one-off scheduling from recurring scheduling; together they support treating tasks as the scoped-work unit and keeping direct threads clean. Source paths: `references/openclaw/docs/automation/cron-vs-heartbeat.md`, `references/openclaw/docs/automation/cron-jobs.md`, `references/pi-mono/packages/mom/docs/events.md`, `references/nanobot/nanobot/skills/cron/SKILL.md`, `references/nanoclaw/docs/SPEC.md`.
- 2026-03-28 [CODE] Simplified the recurring-task runtime model to one JSON file per task under `~/skarbot-state/tasks/` with a status field, then removed recurring-task approvals in favor of one `tasks` skill plus direct file edits. Sources: `docs/runtime/tasks.md`, `.agent/execplans/draft/EP-2026-03-28__skarbot-foundation.md`.
- 2026-03-28 [TOOL] Runtime-skill review: `memory` is the strongest second built-in skill candidate after `tasks`; NanoBot packages it as a lightweight always-on skill, while OpenClaw treats memory as a larger subsystem. Source paths: `references/nanobot/nanobot/skills/memory/SKILL.md`, `references/openclaw/docs/concepts/memory.md`.
- 2026-03-28 [CODE] Split the large `project-definition` doc into focused docs and recorded the filesystem-first `memory` skill defaults in `docs/runtime/memory.md`. Sources: `docs/overview.md`, `docs/runtime/memory.md`, `docs/README.md`.
- 2026-03-28T20:43Z [TOOL] Memory-scope follow-up: `mom` uses shared plus channel-scoped memory, `nanobot` uses a single `MEMORY.md` plus append-only history, and `nanoclaw` relies on per-group files; together they support a simple Skarbot split between deployment memory, per-user memory, and task-attached context. Source paths: `references/pi-mono/packages/mom/CHANGELOG.md`, `references/nanobot/nanobot/skills/memory/SKILL.md`, `references/nanoclaw/groups/main/CLAUDE.md`, `references/oh-my-pi/docs/memory.md`.
- 2026-03-28 [TOOL] Task-identifier review: `mom` uses the filename itself as the event identifier, OpenClaw uses a stable `jobId`, and NanoClaw addresses scheduled tasks by explicit task IDs; the references support either filename-canonical or explicit-ID storage, but they do not point to a single dominant answer. Source paths: `references/pi-mono/packages/mom/docs/events.md`, `references/openclaw/docs/automation/cron-jobs.md`, `references/nanoclaw/docs/SPEC.md`.
- 2026-03-28 [TOOL] Task-filename review: `mom` recommends descriptive `.json` filenames and uses the filename directly during execution, while OpenClaw and NanoBot expose stable job IDs separately from human-facing names; this suggests Skarbot can stay filename-canonical, but still needs to choose between human-readable slugs and opaque stable names. Source paths: `references/pi-mono/packages/mom/docs/events.md`, `references/openclaw/docs/automation/cron-jobs.md`, `references/nanobot/nanobot/skills/cron/SKILL.md`.
- 2026-03-28 [TOOL] Filename-stability review: `mom` uses filenames directly in timers and execution messages, so rename semantics are heavier than metadata edits, while OpenClaw and NanoBot avoid rename concerns by separating stable job IDs from human-facing names; this supports keeping Skarbot filenames stable once created. Source paths: `references/pi-mono/packages/mom/docs/events.md`, `references/openclaw/docs/automation/cron-jobs.md`, `references/nanobot/nanobot/skills/cron/SKILL.md`.
- 2026-03-28 [TOOL] Title-field review: `mom` and NanoBot lean on filename plus task text/message without a separate human-facing title field, while OpenClaw keeps a distinct `name` alongside stable identifiers and task messages; the references support either approach, but keeping an explicit title is mainly useful when the filename stays stable and the human-facing label may change. Source paths: `references/pi-mono/packages/mom/docs/events.md`, `references/openclaw/docs/automation/cron-jobs.md`, `references/nanobot/nanobot/skills/cron/SKILL.md`.
- 2026-03-28 [TOOL] Instructions-field review: `mom` uses a single `text` string, NanoBot uses a single `message` string, OpenClaw uses a single `message` plus optional separate `name`, and NanoClaw uses a single task `prompt`; the references lean toward one plain instruction string rather than a structured instruction object in first-pass task storage. Source paths: `references/pi-mono/packages/mom/docs/events.md`, `references/openclaw/docs/automation/cron-jobs.md`, `references/nanobot/nanobot/skills/cron/SKILL.md`, `references/nanoclaw/docs/SPEC.md`.
- 2026-03-28 [TOOL] Task-memory-shape review: the reference projects mostly store durable memory as plain text or markdown rather than structured nested objects; `nanobot` uses `MEMORY.md`, `nanoclaw` uses `CLAUDE.md`, and OpenClaw keeps richer state outside the task prompt text. This suggests Skarbot should prefer a very small memory shape if it stays inside task JSON. Source paths: `references/nanobot/nanobot/skills/memory/SKILL.md`, `references/nanoclaw/docs/SPEC.md`, `references/openclaw/docs/automation/cron-jobs.md`.
- 2026-03-28 [TOOL] Task-metadata review: `mom` leans on filesystem mtime and file presence for lifecycle, OpenClaw keeps job storage and run history outside the user-facing payload, and NanoClaw exposes run history separately from task definitions; together they suggest explicit `created_at` / `updated_at` fields are optional rather than foundational for the first task-file schema. Source paths: `references/pi-mono/packages/mom/docs/events.md`, `references/openclaw/docs/automation/cron-jobs.md`, `references/nanoclaw/docs/SPEC.md`.
- 2026-03-28 [TOOL] Owner-field review: the references usually target routes or sessions with stable internal handles such as `channelId`, `sessionTarget`, `chat_id`, or `sender_id`, rather than storing mutable contact details as the main control field. This suggests Skarbot should prefer a stable internal user id for task ownership, with email and phone linked elsewhere in runtime state. Source paths: `references/pi-mono/packages/mom/docs/events.md`, `references/openclaw/docs/automation/cron-jobs.md`, `references/nanobot/nanobot/session/manager.py`, `references/nanoclaw/docs/SPEC.md`.
- 2026-03-28 [TOOL] `exe.dev` docs confirm that a public proxy can still force login through `/__exe.dev/login`, authenticated requests then receive stable `X-ExeDev-UserID` and `X-ExeDev-Email` headers, inbound email can be delivered to `~/Maildir/new/`, and the VM can send outbound email to the VM owner through the metadata gateway. This supports Skarbot-managed membership on top of `exe.dev` identity and owner notifications without introducing Resend in v1. Sources: `https://exe.dev/docs/all`, `https://exe.dev/docs/login-with-exe`, `https://exe.dev/docs/sharing`.
- 2026-03-29T05:06Z [TOOL] `pi` and OpenClaw both treat reusable tools as code-registered extensions or plugins, but those extensions commonly shell out to colocated or system executables; this supports Skarbot moving per-user tools to SDK-native `pi` packages rather than `tool.json` executable manifests. Source paths: `references/pi-mono/packages/coding-agent/docs/extensions.md`, `references/pi-mono/packages/coding-agent/docs/packages.md`, `references/pi-mono/packages/coding-agent/examples/extensions/ssh.ts`, `references/pi-mono/packages/coding-agent/examples/extensions/notify.ts`, `references/openclaw/extensions/tlon/index.ts`, `references/openclaw/extensions/lobster/src/lobster-tool.ts`.
- 2026-03-29 [TOOL] OpenClaw creates a fresh resource loader and `createAgentSession(...)` inside each embedded run attempt, while `pi` also supports immediate runtime tool registration and explicit reloads; together this supports Skarbot resolving active user capabilities per run or turn instead of only once for a long-lived session. Source paths: `references/openclaw/src/agents/pi-embedded-runner/run/attempt.ts`, `references/pi-mono/packages/coding-agent/CHANGELOG.md`, `references/pi-mono/packages/coding-agent/docs/extensions.md`.
- 2026-03-29 [TOOL] `pi` explicitly allows one extension to register multiple tools with shared state, and its resource loader flags duplicate tool names across extensions as conflicts. This supports multi-tool capability bundles in Skarbot while still pushing toward globally unique registered tool names. Source paths: `references/pi-mono/packages/coding-agent/docs/extensions.md`, `references/pi-mono/packages/coding-agent/src/core/resource-loader.ts`, `references/openclaw/extensions/tavily/index.ts`, `references/openclaw/extensions/memory-core/index.ts`.
- 2026-03-29 [TOOL] `pi` resource loading exposes deterministic diagnostics for resource conflicts, and OpenClaw exposes `inspect` and `doctor` surfaces for plugin diagnostics. This supports Skarbot using a deterministic system validation tool before capability promotion rather than relying on model judgment alone. Source paths: `references/pi-mono/packages/coding-agent/src/core/resource-loader.ts`, `references/openclaw/docs/tools/plugin.md`.
- 2026-03-29 [TOOL] `pi` supports temporary extension and skill path injection through `additionalExtensionPaths` and `additionalSkillPaths`, while OpenClaw groups plugin management behind `plugins list|inspect|...`; together these support a small grouped `capabilities` task tool plus candidate preview limited to the linked task thread. Source paths: `references/pi-mono/packages/coding-agent/docs/sdk.md`, `references/pi-mono/packages/coding-agent/src/core/resource-loader.ts`, `references/openclaw/docs/tools/plugin.md`, `references/openclaw/src/auto-reply/reply/plugins-commands.ts`.
- 2026-03-29 [TOOL] The `pi` substrate already includes OpenAI Codex subscription auth as a built-in OAuth provider in `@mariozechner/pi-ai`, with provider id `openai-codex`, programmatic `loginOpenAICodex`, OAuth token refresh helpers, and Codex Responses transport support; however, credential storage is explicitly the caller's responsibility. OpenClaw builds on this with `openai-codex/<model>` configuration and stores OAuth profiles in its own state layer. Source paths: `references/pi-mono/packages/ai/README.md`, `references/openclaw/docs/concepts/model-providers.md`, `references/openclaw/docs/concepts/oauth.md`.
- 2026-03-29 [TOOL] `pi-coding-agent` already supports per-session model switching, usage and cost reporting, and persisted model changes through session history: `/model` and `/session` are built-in UI surfaces, `AgentSession` exposes `setModel()` and `cycleModel()`, `session-manager` records `model_change` entries, and session restore derives the current model from history. This supports Skarbot using one deployment default model plus per-thread remembered current models without inventing a second model-persistence layer. Source paths: `references/pi-mono/packages/coding-agent/README.md`, `references/pi-mono/packages/coding-agent/docs/session.md`, `references/pi-mono/packages/coding-agent/src/core/agent-session.ts`, `references/pi-mono/packages/coding-agent/src/core/session-manager.ts`, `references/pi-mono/packages/ai/src/models.ts`.
- 2026-03-29 [TOOL] OpenClaw cron docs treat scheduled work mainly as the same underlying agent-turn machinery with different session target, delivery, and optional model override rather than as a distinct agent type. This supports Skarbot collapsing runtime composition to two execution profiles, `direct-thread` and `task-thread`, instead of inventing extra profiles for scheduled or capability-review cases. Source paths: `references/openclaw/docs/automation/cron-jobs.md`, `references/openclaw/docs/automation/cron-vs-heartbeat.md`.
- 2026-03-29 [TOOL] Approval UX review across the reference set shows that strong implementations use explicit ids or codes plus explicit verbs, not fuzzy natural-language approvals: OpenClaw uses `openclaw pairing approve <channel> <code>` and `/approve <id> allow-once|allow-always|deny`, `pi` and `oh-my-pi` expose UI confirmation primitives and tool-mediated plan approval rather than free-text parsing, and NemoClaw uses explicit operator approve or deny actions in the TUI. This supports Skarbot standardizing on approval ids with explicit `approve`, `deny`, and `feedback` actions across direct-thread and email surfaces. Source paths: `references/openclaw/README.md`, `references/openclaw/src/pairing/pairing-messages.ts`, `references/openclaw/src/auto-reply/reply/commands-approve.ts`, `references/openclaw/src/infra/exec-approval-reply.ts`, `references/openclaw/src/infra/plugin-approvals.ts`, `references/pi-mono/packages/coding-agent/docs/extensions.md`, `references/pi-mono/packages/coding-agent/examples/extensions/permission-gate.ts`, `references/oh-my-pi/packages/coding-agent/src/prompts/tools/exit-plan-mode.md`, `references/nemoclaw/docs/network-policy/approve-network-requests.md`.
- 2026-03-29 [TOOL] Standard-tool inventory across the reference set converges on a small core plus product-specific overlays: `pi` bakes in coding tools (`read`, `write`, `edit`, `bash`, optionally `grep`, `find`, `ls`) plus model/session/reload surfaces and extension UI primitives; `oh-my-pi` layers workflow tools like `ask`, `task`, `todo-write`, `exit_plan_mode`, browser/fetch, LSP, AST search/edit, Python, and calculator; OpenClaw adds product tools for messaging, cron, sessions, subagents, web search/fetch, browser, canvas, nodes, image/PDF, TTS, and gateway control; NanoBot ships a smaller product set with filesystem, shell `exec`, web search/fetch, message, spawn, cron, and MCP; NanoClaw and NemoClaw contribute more orchestration/security patterns than reusable end-user tool catalogs. This suggests Skarbot v1 should keep `pi`'s coding core, add a lightweight planning/task-management surface, keep web search/fetch high on the list, and defer heavier product surfaces like browser, nodes, gateway, and MCP unless the product shape clearly requires them. Source paths: `references/pi-mono/packages/coding-agent/README.md`, `references/pi-mono/packages/coding-agent/docs/extensions.md`, `references/pi-mono/packages/coding-agent/docs/sdk.md`, `references/oh-my-pi/packages/coding-agent/src/prompts/tools/ask.md`, `references/oh-my-pi/packages/coding-agent/src/prompts/tools/task.md`, `references/oh-my-pi/packages/coding-agent/src/prompts/tools/todo-write.md`, `references/oh-my-pi/packages/coding-agent/src/prompts/tools/exit-plan-mode.md`, `references/openclaw/README.md`, `references/openclaw/src/agents/tools/message-tool.ts`, `references/openclaw/src/agents/tools/cron-tool.ts`, `references/openclaw/src/agents/tools/sessions-spawn-tool.ts`, `references/openclaw/src/agents/tools/web-fetch.ts`, `references/openclaw/src/agents/tools/web-search.ts`, `references/openclaw/src/agents/tools/tts-tool.ts`, `references/nanobot/nanobot/agent/loop.py`, `references/nanobot/nanobot/agent/tools/filesystem.py`, `references/nanobot/nanobot/agent/tools/shell.py`, `references/nanobot/nanobot/agent/tools/web.py`, `references/nanobot/nanobot/agent/tools/message.py`, `references/nanobot/nanobot/agent/tools/spawn.py`, `references/nanobot/nanobot/agent/tools/cron.py`, `references/nanobot/nanobot/agent/tools/mcp.py`, `references/nanoclaw/docs/SDK_DEEP_DIVE.md`, `references/nemoclaw/docs/network-policy/approve-network-requests.md`.
- 2026-03-29 [TOOL] `pi-coding-agent` already gives Skarbot most of the coding substrate out of the box via SDK/session APIs: `codingTools` = `read`, `bash`, `edit`, `write`; `readOnlyTools` = `read`, `grep`, `find`, `ls`; model/session switching and persistence are built into `AgentSession`; and the extension system already ships reusable patterns/examples for a todo/planning tool (`todo.ts`), dynamic tool registration, subagents (`subagent/`), runtime reload, protected paths, permission gates, send-user-message, tool toggling, model status, and preset-based model/tool changes. This suggests Skarbot should avoid redefining the coding core and should prefer adapting existing `pi` extension patterns for planning, messaging/ask-user, subagents, and model control before inventing bespoke implementations. Source paths: `references/pi-mono/packages/coding-agent/docs/sdk.md`, `references/pi-mono/packages/coding-agent/README.md`, `references/pi-mono/packages/coding-agent/docs/extensions.md`, `references/pi-mono/packages/coding-agent/examples/extensions/todo.ts`, `references/pi-mono/packages/coding-agent/examples/extensions/send-user-message.ts`, `references/pi-mono/packages/coding-agent/examples/extensions/subagent`, `references/pi-mono/packages/coding-agent/examples/extensions/model-status.ts`, `references/pi-mono/packages/coding-agent/examples/extensions/preset.ts`, `references/pi-mono/packages/coding-agent/examples/extensions/tools.ts`, `references/pi-mono/packages/coding-agent/examples/extensions/permission-gate.ts`, `references/pi-mono/packages/coding-agent/examples/extensions/protected-paths.ts`.
- 2026-03-29 [TOOL] Planning references support a much smaller Skarbot `plan(...)` surface than the earlier phased proposal: `pi`'s `todo.ts` shows the right persistence model (thread/session-backed current list state), oh-my-pi's `todo-write` mainly contributes the rule that progress should update incrementally and clearly, and `pi`'s full `plan-mode` example is materially heavier than the v1 need. This supports a single active thread-local plan with just `get/create/update` semantics rather than phases, plan-mode gating, or separate plan artifacts. Source paths: `references/pi-mono/packages/coding-agent/examples/extensions/todo.ts`, `references/oh-my-pi/packages/coding-agent/src/prompts/tools/todo-write.md`, `references/pi-mono/packages/coding-agent/examples/extensions/plan-mode/index.ts`.
- 2026-03-29 [TOOL] Web and product-tool references support a small standard Skarbot system layer rather than a full OpenClaw-style control plane: OpenClaw and NanoBot both treat `web_search` and `web_fetch` as first-class lightweight web tools, while `pi` already covers the coding core and extension substrate. This supports documenting `web_search` and `web_fetch` as shared system tools on top of the `pi` substrate instead of inventing a heavier browser-first surface in v1. Source paths: `references/openclaw/src/agents/tools/web-search.ts`, `references/openclaw/src/agents/tools/web-fetch.ts`, `references/nanobot/nanobot/agent/tools/web.py`, `references/pi-mono/packages/coding-agent/docs/sdk.md`.
- 2026-03-29 [TOOL] `pi` already provides the needed substrate for a minimal grouped model-control tool: `AgentSession.setModel(...)`, `cycleModel(...)`, `getSessionStats()`, persisted `model_change` entries, and built-in `/model` and `/session` surfaces. This supports Skarbot defining a small thread-scoped `model()` / `model(list)` / `model(set, provider, model)` tool rather than inventing a larger model-governance subsystem in v1. Source paths: `references/pi-mono/packages/coding-agent/README.md`, `references/pi-mono/packages/coding-agent/src/core/agent-session.ts`, `references/pi-mono/packages/coding-agent/docs/session.md`, `references/pi-mono/packages/coding-agent/src/modes/rpc/rpc-mode.ts`, `references/pi-mono/packages/coding-agent/examples/extensions/model-status.ts`, `references/pi-mono/packages/coding-agent/examples/extensions/preset.ts`.
- 2026-03-29 [TOOL] Scheduling references consistently separate one-shot and recurring behavior, and timezone defaults belong to runtime or user configuration rather than being invented ad hoc by the model: OpenClaw and NanoBot expose grouped scheduler actions like list/add/remove or list/add/update/remove, `mom` deletes one-shot events after execution while recurring events persist, and both `mom` and NanoBot default unspecified scheduling timezones from runtime configuration. This supports Skarbot making `schedule(...)` a grouped direct-thread tool with list/set/edit/delete, recurring schedules bound to reusable task threads, one-off schedules mapped to throwaway task threads, and default timezone sourced from the user's configured IANA timezone. Source paths: `references/openclaw/src/agents/tools/cron-tool.ts`, `references/openclaw/docs/automation/cron-jobs.md`, `references/nanobot/nanobot/agent/tools/cron.py`, `references/nanobot/nanobot/skills/cron/SKILL.md`, `references/nanobot/README.md`, `references/pi-mono/packages/mom/docs/events.md`.
- 2026-03-29 [TOOL] `pi` expects an `auth.json`-compatible credential store and leaves OAuth credential persistence to the caller, while OpenClaw only adds a more complex auth-profile layer because it needs multi-agent and multi-profile routing. This supports Skarbot using a single host-managed `auth.json`-compatible file for deployment-wide Codex auth, but keeping it outside portable `~/skarbot/state`. Source paths: `references/pi-mono/packages/coding-agent/src/core/auth-storage.ts`, `references/pi-mono/packages/coding-agent/docs/sdk.md`, `references/pi-mono/packages/ai/README.md`, `references/openclaw/docs/concepts/oauth.md`, `references/openclaw/src/agents/pi-auth-json.ts`, `references/openclaw/src/agents/pi-model-discovery.auth.test.ts`.
- 2026-03-29 [TOOL] Compaction research across `pi`, `mom`, and OpenClaw converges on one low-risk baseline: persistent append-only thread history on disk, automatic threshold compaction plus overflow-recovery compaction, rolling summary accumulation through previous compaction summaries, truncated tool serialization for summarization inputs, and a recent verbatim tail preserved after the compaction point. OpenClaw adds worthwhile later hardening such as identifier preservation, chunked safeguard summarization, timeouts, and optional pre-compaction memory flush, but the agreed Skarbot baseline is the simpler rolling-summary-plus-recent-tail model. Source paths: `references/pi-mono/packages/coding-agent/docs/compaction.md`, `references/pi-mono/packages/coding-agent/src/core/compaction/compaction.ts`, `references/pi-mono/packages/coding-agent/src/core/agent-session.ts`, `references/pi-mono/packages/mom/README.md`, `references/openclaw/src/agents/compaction.ts`, `references/openclaw/src/agents/pi-extensions/compaction-safeguard.ts`, `references/openclaw/docs/concepts/compaction.md`, `references/openclaw/docs/reference/session-management-compaction.md`, `references/openclaw/docs/concepts/session-pruning.md`, `references/openclaw/docs/concepts/memory.md`, `references/openclaw/docs/gateway/configuration-reference.md`.
- 2026-03-29 [TOOL] Memory follow-up: the strongest common thread across `mom`, NanoBot, and OpenClaw is that durable memory is plain files in the workspace, while richer memory tools are optional convenience layers. This supports simplifying Skarbot to workspace-local `MEMORY.md` files maintained through the normal coding tools and deferring searchable thread history to a separate future tool instead of inventing a dedicated `memory(...)` tool now. Source paths: `references/pi-mono/packages/mom/README.md`, `references/nanobot/nanobot/agent/memory.py`, `references/nanobot/nanobot/agent/context.py`, `references/openclaw/docs/concepts/memory.md`.
- 2026-04-08 [TOOL] Follow-up compaction review changed the current runtime-doc direction: Skarbot now explicitly adopts a silent pre-compaction memory flush turn before normal compaction because that matches the repo's preference for durable housekeeping before context is compressed away. The current runtime draft uses `reserveTokens: 20000`, `keepRecentTokens: 20000`, and `memoryFlushThreshold: 4000`, with one memory flush per compaction cycle. Source paths: `references/pi-mono/packages/coding-agent/docs/compaction.md`, `references/pi-mono/packages/coding-agent/src/core/compaction/compaction.ts`, `references/pi-mono/packages/coding-agent/src/core/compaction/utils.ts`, `references/openclaw/docs/concepts/compaction.md`, `references/openclaw/docs/concepts/memory.md`, `references/openclaw/docs/reference/session-management-compaction.md`, `references/openclaw/docs/concepts/session-pruning.md`, `references/pi-mono/packages/mom/README.md`, `references/nemoclaw/docs/workspace/workspace-files.md`.
- 2026-04-09 [TOOL] Approval follow-up review reinforced the current direction: explicit ids plus explicit verbs are still the best pattern, and OpenClaw's approval routing/code paths are useful mainly as UX references rather than storage shape references. The pending Skarbot runtime draft keeps one shared approval system with `user` and `admin` approver classes, UUID ids, explicit `approve|deny|feedback` actions, pause-on-approval behavior, and resume semantics wired back to the source thread or task deterministically. Source paths: `docs/runtime/approvals.md`, `docs-v5/runtime.md`, `docs-v5/architecture.md`, `references/openclaw/docs/cli/approvals.md`, `references/openclaw/docs/tools/exec-approvals.md`, `references/openclaw/src/infra/exec-approval-reply.ts`, `references/openclaw/src/infra/exec-approval-forwarder.ts`, `references/openclaw/docs/concepts/session.md`, `references/pi-mono/packages/coding-agent/docs/session.md`, `references/pi-mono/packages/coding-agent/docs/sdk.md`.
